import { createPaginator } from "@smithy/core";
import { ListSolutionsCommand, } from "../commands/ListSolutionsCommand";
import { PartnerCentralSellingClient } from "../PartnerCentralSellingClient";
export const paginateListSolutions = createPaginator(PartnerCentralSellingClient, ListSolutionsCommand, "NextToken", "NextToken", "MaxResults");
